<!DOCTYPE html>
<html class="no-js" lang="">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
</head>

<body>
    <ul>
        <% params.assetNames.forEach(function(assetName){ %>
            <li><a href="<%= assetName %>"><%= assetName %></a></li>
        <% }) %>
    </ul>
</body>

</html>
