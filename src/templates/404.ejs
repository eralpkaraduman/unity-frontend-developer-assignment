<!DOCTYPE html>
<html lang="en">
  <% include partials/head %>
  <body>
    <ul>
      <% assetNames.forEach(function(assetName){ %>
      <li><a href="<%= assetName %>"><%= assetName %></a></li>
      <% }) %>
    </ul>
  </body>
</html>
